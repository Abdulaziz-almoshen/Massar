# Design: Massar — the complete platform, implemented

Generated by /office-hours 2026-09-03; amended the same day by /autoplan
(CEO -> design -> DX -> engineering, eight independent voices).
Branch: master · Repo: Abdulaziz-almoshen/Massar (engine: Abdulaziz-almoshen/massar-engine)
Status: **AMENDED AFTER REVIEW** · Mode: Startup (intrapreneurship)
Supersedes: `docs/designs/massar-v2-commercial-crm.md` and `docs/designs/massar-commercial-platform.md`

**Reconciled 2026-09-04 against `docs/designs/massar-vision-ux-and-pitch.md`.** That document is
the source of truth for the **first eight weeks and the pitch**; this one is the architecture those
weeks build inside. Where they disagree, the vision doc wins on sequencing and on what a user sees;
this one wins on schema and safety. Three things were folded in from the founder archive
(`docs/artifacts/masar-archive/`) that this plan had been missing entirely: the **eight weighted
sales stages**, the **~28-value dependent outcome taxonomy**, and **`الإدارة المسؤولة`** on every
engagement. A **targets** model was added; the plan previously had no concept of one.

**Autonomous run.** The founder instructed: no confirmation questions, take the most recommended
and highest-completeness option. Every decision here was auto-taken on that instruction. **One
decision was not auto-taken** and is recorded at the end under "The one open question", because
all eight review voices recommended changing a direction the founder gave in his own words twice.

---

## What the founder asked for

The full platform: *"Everything with the WhatsApp engine, with the campaign configuration, with
the responses, with the profiling."* Plus the product hub, where a product manager uploads
material the AI agent uses, a rep opens the same product to learn it before a call, files can be
uploaded, and a sales deck is generated from that material with a critic.

**That scope is unchanged in this document.** The review did not cut it. It corrected what the
document said about the code, fixed defects that made it unexecutable, and added the prerequisites
the first estimate omitted.

## What the review changed

| # | The first draft said | The truth, verified in source |
|---|---|---|
| 1 | "No global send gate. Zero matches for any `canSend`-shaped guard." | **Wrong.** `src/outbound.ts` is exactly that gate, written after a QA finding, and its header already claims to be "the single door". It has 2 callers out of **15** send sites. `scripts/check-optout.mjs:92` prints the bypass on every build: *"~13 other call sites reach Gupshup"*. |
| 2 | "Opt-out is checked in one place inside a route's loop." | Understated. Opt-out is enforced in `outbound.ts`, in `db.ts:1072`, in `agent.ts`'s STOP short-circuit, and again **inline** in the launch loop (`index.ts:359`). The problem is duplication, not absence. |
| 3 | Phase 0 builds a gate in 3 weeks. | Phase 0 is mostly **demolition**: collapse 13 bypassing call sites into the door that exists. |
| 4 | "A double-run produces one outbox row, not two." | **Cannot pass.** `db.ts:743 createCampaign` INSERTs a fresh `BIGSERIAL` per POST, so a key containing `campaign_id` differs between runs and the UNIQUE never fires. |
| 5 | "Every phase behind the existing 16-step gate." | The coverage gate covers **4 files, 1,004 of 15,780 lines**, and its `include` list is enumerated, so a *new* `*-domain.ts` stays outside it too. |
| 6 | `UNIQUE (account_id, product_id) WHERE closed_at IS NULL` | **Invalid PostgreSQL.** A partial predicate cannot attach to a table constraint. And live `opportunities` already contains duplicates, so the index would fail on real data. |
| 7 | `product_assets (id, product_version_id, ...)` | **Redefines an existing table** whose PK is `product TEXT` and which holds the `BYTEA` that serves files to WhatsApp. |
| 8 | Phase 0 ships a breaker that trips on quality drop. | The `templates` table carrying `quality` does not exist until **phase 6**. That half of phase 0 could not function. |
| 9 | "No parallel run, no cutover." | Phase 2 moves every live screen onto new tables while state sits in `contacts`, `entities`, `opportunities`, `opp_auto`. **That is a cutover.** |
| 10 | 29 weeks. | **38.** The estimate omitted a migration system, a database test harness, CI, and the file-storage decision — all gating, none optional. |

## Five defects that exist in production today

Found while checking the plan's claims. None is caused by this plan; all are made worse by it.

1. **An inbound «إيقاف» can be lost.** `index.ts:41` acks the webhook *before* persisting
   anything. A process death after the ack loses the event permanently and the number stays
   sendable. No suppression ledger fixes this, because the loss precedes any write.
2. **A provider timeout sends the message twice.** `gupshup.ts:44` is a bare `fetch` with no
   `AbortSignal` (there is none anywhere in the codebase), and `agent.ts:1503-1509` catches a
   failed quick-reply and re-sends the same content as plain text. If Gupshup accepted the first
   one, the customer gets both.
3. **A failed migration reports healthy.** `db.ts:321` catches it and starts in memory-only mode;
   `/health` returns `ok: true` unconditionally.
4. **The webhook accepts forged POSTs when the token is unset.** `if (cfg.webhookToken && ...)` —
   an empty token makes the guard false. `configReport()` prints "webhook is unauthenticated!"
   and the app boots anyway.
5. **Per-recipient launch failures are computed, returned, and dropped.** `index.ts:428` returns
   `failed: [...]`; nothing in `dashboard.ts` reads it. The UI shows only «أُرسلت N من M».

## The finding that still reorders everything

Not "there are no brakes" — there are, in four places, and almost nothing uses them.

**A door standing in a field is worse than no door**, because its existence reads as coverage.
`outbound.ts` makes, in its own header, the exact promise this plan made for its new gate: *"This
module holds the POLICY so it cannot be re-derived per call site."* Within one cycle the
highest-traffic caller went around it, and the guard script has been announcing that on every
build since.

**A second policy module will hold no better than the first.** The seam has to be structural: the
gate becomes the only module that can name `gupshup.send*`, and a build check fails on any other
import. That is the difference between a convention and a mechanism, and it is the single most
valuable change in this document.

**And per-template pacing still matters.** Meta rates and paces each template individually, so a
degrading template throttles a campaign while the account looks healthy. But the pacing rule cuts
both ways: a template can only *leave* pacing by sending, so "the wizard refuses a paced template"
would mean no new template ever reaches Active-High. Pacing means a controlled low-volume state,
never blanket refusal.

## Premises, with a falsification test each

The first draft asserted six premises. Each now carries the test that would prove it wrong.

| # | Premise | Falsified if |
|---|---|---|
| 1 | Commercial semantics are the missing thing | A rep with the queue in hand still works from the sheet after three weeks |
| 2 | The outbound path needs structural brakes first | The one-door check finds nothing to collapse (it will find 13) |
| 3 | Template quality is per-template and paced | Two templates never need a lifecycle a spreadsheet column could hold |
| 4 | Profiling is empty and the fix is upstream | Fact coverage stays near zero after the enriched import |
| 5 | Product knowledge needs versioning and an audience split | No rep opens a product page in a month |
| 6 | Every artefact carries its source or it does not ship | — this one is a rule, not a hypothesis |

**Premise 1 has never been tested against a person.** No rep, PM or clinic is quoted anywhere in
25 days of records; every stakeholder quote is the founder's. That is the weakest joint in this
document and no amount of schema fixes it.

---

## Architecture

Everything lands **inside `massar-engine`**. No second application.

### Phase 0a — foundations (nothing above is safe without these)
```
schema_migrations   numbered files, a version table, an advisory lock, run as a Fly
                    release_command so a bad migration FAILS THE DEPLOY instead of
                    half-booting into memory-only mode.  Today: one 230-line SQL
                    string run at startup AND by a 30s reconnect timer.
pg_dump             in the release command. There is no backup anywhere in the repo.
Testcontainers      a Postgres harness. The outbox's whole value is a concurrency
                    property and no pure-function test can observe one.
coverage.include    becomes a glob over src/*-domain.ts plus the safety modules.
                    Two lines. Today a NEW tested domain module stays outside the gate.
CI                  a GitHub Action running npm run check on push. There is none in
                    either repo, and npm run check gates only npm run deploy.
SIGTERM handler     there is none. A deploy mid-send strands rows with unknown outcomes.
AbortSignal         fetch timeout of 15s in gupshup.ts. A timeout is classified
                    `unknown`, NEVER `failed` — a timed-out send usually succeeded.
/health             returns ok:false when the database is enabled and not connected.
```

### Phase 0 — the one door
```
send-gate.ts        the ONLY module that may import the Gupshup send functions.
                    Enforced by check-onedoor.mjs in npm run check, built on the
                    technique in check-props.mjs (which already imports
                    dist/dashboard.js and evaluates the client bundle).
                    Exit criterion, stated as a number: bypassing call sites 13 -> 0.

  purpose           marketing | service_reply | consent_ack | internal_alert
                    consent_ack is EXEMPT from suppression — otherwise the gate
                    refuses the confirmation of the opt-out that just wrote the
                    suppression (agent.ts:1170 records, then sends).
                    internal_alert is exempt from customer-window logic.
                    The exemption set is asserted by a test.

  FAIL CLOSED       if the database is unreachable the gate REFUSES. Stated
                    explicitly because this codebase's prevailing idiom is the
                    opposite: fire() forgets writes when disconnected and
                    listOpps/listEntities return [] — a suppression lookup written
                    in the house style would read "no row" as "not suppressed".
                    This trades WhatsApp availability for consent integrity.

suppressions        (phone, reason, source, purpose_scope, consent_wording,
                     created_at, created_by, expires_at)
                    phone is BARE DIGITS, one normalizePhone (audience.ts, tested),
                    CHECK (phone ~ '^[0-9]{8,15}$'), and a boot assertion that
                    writer and reader agree. The first draft wrote `phone` in one
                    table and `phone_e164` in two others; a suppression written
                    +966... and read 966... answers "not suppressed".
                    BACKFILLED from contacts.opted_out as the FIRST migration, and
                    the gate reads BOTH sources until the backfill is proven equal.

outbox              (id, purpose, to_phone, template_id, payload, idempotency_key UNIQUE,
                     state, provider_msg_id, attempts, next_attempt_at,
                     claimed_at, lease_until, worker_id, created_at, sent_at, error)
                    state: queued | sending | sent | failed | suppressed | unknown | dead

  idempotency_key = sha256(template_id | template_version | to_phone
                           | rendered_body | window_bucket)
                    NOT the campaign id. createCampaign mints a fresh BIGSERIAL per
                    POST, so a key containing it never collides. Campaign creation
                    is itself made idempotent by a client Idempotency-Key stored
                    UNIQUE on campaigns.

dispatcher          CARVED OUT of A10's deferral. An outbox with no consumer is a
                    log, not a queue. Claims with FOR UPDATE SKIP LOCKED, holds a
                    lease, recovers after worker death, backs off with jitter,
                    dead-letters, and PRESERVES PER-PHONE ORDER — queue.ts
                    guarantees that today and a naive outbox would discard it.

inbound_events      (wa_message_id UNIQUE, raw, received_at, processed_at)
                    Persisted BEFORE the webhook acks. Today the route acks first
                    and a crash loses the event — including an «إيقاف».
                    The webhook refuses to boot without WEBHOOK_TOKEN, and the token
                    moves out of the query string.

send_audit          append-only: who, what, when, which gate decision, which
                    template version, correlation id.
                    AND IT HAS AN OPERATOR SURFACE. index.ts:428 already returns
                    per-recipient failures and dashboard.ts never reads them; a
                    decision log nobody can see repeats that.
```
The per-template circuit breaker moves to **phase 6**, where its input exists.

### Phase 2 — identity and the commercial spine
```
users, departments, roles, sessions
  sessions.token stored as sha256(token), never raw. Cookie flags, CSRF, login
  rate limiting, lockout, reset — named now, not implied by four column names.
  ADMIN_TOKEN becomes break-glass-only and audited; the ?token= URL entry path
  (dashboard.ts:559, persisted to localStorage) is deleted in this phase.

accounts            REPLACES `entities`. The first draft never named the table it
                    replaces. Migration is expand -> backfill -> dual-read ->
                    writer handoff -> contract, one writer per table at all times.
account_contacts    (account_id, name, role_title, phone, email, is_primary)
                    partial unique on one primary per account.
phone_routing       (phone, valid_from, valid_to, account_contact_id)
                    partial unique on the ACTIVE row. The first draft's
                    (phone PK) contradicted account_contacts in the same block:
                    one permitted a phone under two accounts, the other forbade it.
                    A reassigned Saudi mobile needs the validity window or a
                    stranger's replies land, uncorrectably, in an append-only table.
                    An ambiguous route is surfaced to a human, never guessed.
handoffs            (+ sla_days, anchored to Asia/Riyadh)
handoff_items       partial failure is first-class: 47 succeed, 3 fail, each fixable
product_tracks      REPLACES `opportunities`. pipeline_id + stage_id now have
                    tables to point at (below), with a composite constraint tying
                    the stage to its pipeline and the pipeline to the product.
pipelines           per-product stage sets as data (A11).
pipeline_stages     (pipeline_id, key, label_ar, weight_pct, entry_criterion_ar, sort)
                    SEEDED FROM THE FOUNDER ARCHIVE, not invented. The real Lean
                    model is EIGHT stages carrying probability weights, recovered
                    from docs/artifacts/masar-archive/:

                      تواصل أولي            10%   وصلنا لصاحب القرار وأبدى اهتمامًا
                      اكتشاف الحاجة         25%   تأكدت الحاجة والحجم والميزانية
                      عرض المنتج            45%   قُدّم المنتج وقبل العميل القيمة
                      التقييم التقني        65%   اجتاز التكامل مع صحة/صحتي والأمن
                      عرض السعر             80%   عرض سعر مقبول مبدئيًا
                      التفاوض والاعتماد     90%   توافق على الشروط ودخول التعاقد
                      إغلاق – ربح          100%   تم التوقيع
                      إغلاق – خسارة          0%   اعتذر العميل (سجّل السبب)

                    The engine ships six unweighted stages today
                    (contact|present|tech|negotiate|won|lost). WITHOUT weights no
                    weighted forecast is computable, and a forecast is the number a
                    sales director is measured on.

                    **The mapping is total, and it keeps the existing keys.** Six map
                    one-to-one and two are genuinely new, so no live row is reclassified:

                      contact   -> contact      (10%)
                      NEW       -> discover     (25%)   اكتشاف الحاجة
                      present   -> present      (45%)
                      tech      -> tech         (65%)
                      NEW       -> quote        (80%)   عرض السعر
                      negotiate -> negotiate    (90%)
                      won       -> won         (100%)
                      lost      -> lost          (0%)

                    Keeping the keys means `OPP_STALL_STAGES = ["tech","negotiate"]`
                    (opps-domain.ts:53) and `isWonStage` (opps-domain.ts:90) survive
                    unchanged. **Audit `negotiate` before backfill**: some live rows in
                    it may really be the newly separated `quote`.

                    **The enum is duplicated in six places and a `CREATE TABLE IF NOT
                    EXISTS` alters none of them.** All six move in one compatibility
                    sequence or the system half-migrates:
                      db.ts:263   CHECK (stage IN (...)) — must be DROPped and recreated
                      db.ts:935   OPP_STAGES const
                      db.ts:952   the line validator
                      opps-domain.ts:23   the second stage definition
                      opps-domain.ts:221  **serialised into the browser bundle** — a
                                          cached old bundle submits six-stage
                                          assumptions after the server has moved
                      opps-domain.ts:53,90  stall and won semantics
                    Constraints on the new tables: UNIQUE (pipeline_id, key) and
                    `weight_pct BETWEEN 0 AND 100`. Contract the old CHECK only after
                    stale browser bundles can no longer be in flight.
engagements         append-only, WITH corrects_engagement_id — a new row cannot
                    correct an old one if it cannot name it — and
                    UNIQUE (source, external_id) so a replayed webhook does not
                    duplicate the transcript. Several agent guards read the
                    transcript to survive restarts, so duplication changes agent
                    behaviour silently.
outcomes            (stage_id, key, label_ar, reason_key, reason_ar, next_action_ar,
                     kind ∈ advance|needs_action|lost)
                    DEPENDENT ON STAGE, ~28 values, recovered from the archive. The
                    outcome list changes with the stage — «يحتاج توضيح تقني» belongs
                    to عرض المنتج and nowhere else. `next_action_ar` is a TEMPLATE, not
                    the action; the action itself is a row in `actions` below.
                    Colour follows kind: advance = green, needs_action = amber,
                    lost = red, each paired with its label, never colour alone.
form_schemas        (engagements.form_schema_id).

track_stage_events  THE LEDGER THIS PLAN WAS MISSING. Append-only.
                    (id, track_id, from_stage_id, to_stage_id, outcome_id,
                     effective_at TIMESTAMPTZ, recorded_at, actor_id,
                     engagement_id NULL, corrects_event_id NULL)
                    CHECK: outcome.stage_id = from_stage_id.
                    to_stage_id MAY equal from_stage_id — a stage can be assessed
                    without moving.

                    **Three separate findings collapse into this one table.**

                    (a) The outcome was on `engagements` and does not belong there. An
                        engagement is EVIDENCE — a call, a visit, a message. A stage
                        outcome is a DECISION about one product track at one stage. One
                        call can cover two products; a stage can change with no customer
                        contact at all (legal signs off, procurement approves); and a
                        call can produce a stage change days later after internal review.

                    (b) **`achieved` is not computable without it.** Verified in source:
                        `opportunities.stage_at` (db.ts:286) is the moment the stage LAST
                        MOVED and is overwritten on every later change (db.ts:1021), and
                        `close_on` (db.ts:280) is a PLANNED date, not an actual. So the
                        engine today cannot answer "which deals were won in Q1" — the
                        single question the whole targets model rests on. Without this
                        ledger phase 3 is unbuildable.

                    (c) It is where `won_at` lives, as a real TIMESTAMPTZ rather than a
                        BIGINT that means something different every time it is read.

actions             CURRENT responsibility, which is NOT the same fact as history.
                    (id, track_id, stage_event_id, responsible_dept, responsible_person,
                     title_ar, due_at, state ∈ open|done|cancelled, done_at, created_at)
                    responsible_dept ∈ إدارة المنتج · المبيعات · التقنية · القانونية ·
                                        المشتريات · أمن المعلومات

                    **The differentiator lives here, and the first draft put it in the
                    wrong place.** `responsible_dept` on an append-only engagement would
                    record who handled a HISTORICAL interaction — so «أين تتعثّر الصفقات»
                    would list every department that ever touched the deal, stale rows
                    and duplicates included, instead of whose desk it is on NOW.

                    The archive's finding is unchanged and still the reason this product
                    exists: التقييم التقني and التفاوض والاعتماد stall longest because
                    responsibility crosses a department boundary. Corroboration from the
                    engine, found during this review: `OPP_STALL_STAGES` in
                    `opps-domain.ts:53` is already hardcoded to exactly `["tech",
                    "negotiate"]` — the same two stages, identified independently by the
                    prototype months earlier.

                    Handoffs move a book of accounts. An action moves one stalled deal.
                    Both are needed and they are different tables.

  channel: call | visit | whatsapp | email | other
           `campaign` REMOVED — a campaign is a container, WhatsApp is the channel.
           A campaign message IS a WhatsApp message, and the old enum could not
           express that.

  One active track per (account, product) is a RULE in tracks-domain.ts, not a
  table constraint. If a database index is wanted it ships separately as
  CREATE UNIQUE INDEX CONCURRENTLY after a duplicate report, never inside the
  migration string. The first draft's syntax was invalid, would have failed on
  live duplicates, inverted under NULL account_id, and forbade renewals, hospital
  branches, competing quotes and reopened deals.
```

### Phase 3 — surfaces, including targets and the management view

**Targets are new.** The first draft had no concept of one.
```
sectors             (id, name)                      قطاع — the layer ABOVE product
                    The archive's org model is قطاع ← إدارة ← منتج, and `departments`
                    from phase 2 sits between them. The first draft had departments
                    and stopped; an executive dashboard rolls up through SECTORS.
products            gains sector_id and a product-manager owner.

targets             (product_id, year, quarter ∈ 1..4, amount)
                    UNIQUE (product_id, year, quarter)
                    Annual target split quarterly. Entered, not computed.

achieved            NOT A TABLE. Computed from product_tracks won inside the quarter,
                    so it can never drift from the ledger and no one can type a number
                    into it. The whole point of the executive view is that every figure
                    is earned; a stored `achieved` column would be the one hand-entered
                    number on the screen.

forecast            ALSO computed: sum over open tracks of
                    sale_price × qty × years × (1 − discount) × stage.weight_pct.
                    This is why the eight weighted stages are a phase-2 prerequisite
                    and not a nicety. Six unweighted stages produce no forecast at all.

  TWO NAMED MEASURES, because "%" was ambiguous and every colour on the screen
  depends on which one is meant:
    attainment_pct = achieved / target                        <- drives RAG
    coverage_pct   = (achieved + weighted_open_forecast) / target
  Aggregate as SUM(numerator) / SUM(target), NEVER the average of product
  percentages. Define: zero or missing target, currency, rounding, and >100%.
  RAG compares attainment against the ELAPSED share of the period, not the whole
  period — otherwise every product is red in the first week of a quarter.

  REPORTING CALENDAR, stated once so three screens cannot disagree:
    fiscal_year_start_month · timezone Asia/Riyadh · year-label convention
    Quarter boundaries are HALF-OPEN in Riyadh local time:
      Q1 2026 = [2026-01-01 00:00 +03, 2026-04-01 00:00 +03)
    A deal won 2026-03-31 23:30 Riyadh is 20:30Z and belongs to Q1 — reading it
    in UTC would put it in the wrong quarter and the wrong bonus.
    Bucket on `track_stage_events.effective_at`, never on `close_on` (a plan).

  WHAT THE TARGET MEASURES is a business fact, not a schema choice, and it is an
  open question below. `sale_price × qty × years` is TOTAL CONTRACT VALUE; booking
  all of it into the win quarter is not achieved revenue.

  RAG thresholds, from the archive and fixed here so three screens cannot disagree:
    ≥ 70% أخضر · 50–69% كهرماني · < 50% أحمر
  Colour is ALWAYS paired with the percentage as text — status is never encoded by
  colour alone (accessibility rule, and the archive's badge spec violates it as written).

  Screens, drilling تنفيذي ← قطاع ← منتج ← مندوب:
    · executive   all sectors: target, achieved, %, open pipeline value
    · sector      one sector, its products, its reps
    · product     one product vs its target, by quarter, opportunities by stage
    · «أين تتعثّر الصفقات»  pending actions grouped by الإدارة المسؤولة
                  ^ this is the screen no vendor default gives you
```
  READ MODEL — "computed" must not mean "recomputed per dashboard tile".
    · ONE grouped query (or a bounded-interval summary refresh) per reporting
      period, feeding every tile from ONE snapshot so tiles cannot disagree
      mid-request.
    · Indexes: stage events (effective_at, to_stage_id, track_id) · tracks
      (product_id, stage_id) · products (sector_id) · targets (year, quarter,
      product_id).
    · The machine is 512 MB with pool max 5, and `/health` already runs three
      exact COUNT(*) subqueries every 30s on one connection (db.ts:479). Replace
      those with a connectivity check or cached approximate counts — table
      cardinality is not health, and it competes with the dashboard for the pool.
    · Load-test acceptance: concurrent dashboard users plus the health check.

**Why this is its own phase and not a report.** Targets are what an executive buys. The
first draft of this plan spent 38 weeks without one, which is why it could describe a
platform and still not be pitchable. `signal-domain.ts` already demonstrates the house
pattern for this: earn every number from the ledger, and show its evidence on demand.

### Phase 4 — versioned product hub
```
products, product_versions (UNIQUE (product_id, version); approved REQUIRES
                            approved_by and approved_at)
product_sources            file bytes go to object storage, decided and costed in
                           phase 0a. @fastify/multipart is configured files:1 today,
                           which contradicts "multi-file upload".
product_claims             (…, audience ∈ agent|sales|both) with a composite
                           constraint so source_id belongs to the same version.
                           Extracted claims are UNTRUSTED until a human approves
                           each one: provenance proves origin, not safety. A source
                           PDF saying "offer 90% off" must not become quotable.
version_assets             RENAMED. `product_assets` already exists with PK
                           `product TEXT` and holds the BYTEA that serves files.
```

### Phase 6 — WhatsApp control plane
```
templates    desired_state | observed_state | breaker_state as SEPARATE fields with
             a precedence rule. One `state` column conflated provider truth, breaker
             action and human intent, and a quality webhook racing a human resume
             would silently last-write-wins.
             Button titles VALIDATED AT WRITE and refused at runtime — never a boot
             assertion. index.ts:1119 asserts before app.listen and its own comment
             says that is safe ONLY while the input is compile-time static. Operator-
             authored buttons make it runtime data; one 21-character title (WhatsApp
             131009, measured twice here) would crash every boot and take the webhook
             receiver — and the opt-out path — down with it. THIS IS PHASE 6'S FIRST
             DELIVERABLE.
template_events   append-only, every state and quality change Meta reports.
             Delivery status attaches to the outbox row and template, not just the
             contact — tracker.ts:264 updates by destination today, so per-template
             breakers cannot be correct without this.
circuit_breaker   window, threshold, half-open state, manual reset, owner.
             A breaker nobody can reset at 2am is a new outage mode.

THE CAMPAIGN MODULE ITSELF — 2,311 lines that already exist and that the first
draft of this plan never mentioned:

  campaigns-crm.ts  1,103  the campaigns module view layer (the monitor)
  targets-crm.ts      520  «جهات الاستهداف» — the imported book
  segments.ts         278  behavioural segmentation, «التقسيم وإعادة الاستهداف»
  audience.ts         157  Excel/CSV import, Arabic+English header auto-map,
                           KSA phone normalisation, upsert by phone
  templates.ts        253  templates as constants

None of it is thrown away. What happens to each is stated here because phase 2
moves the ground under all of it:

  audience.ts   upserts by phone into `entities`. Phase 2 replaces `entities`
                with `accounts` + `account_contacts` + `phone_routing`, so the
                importer's upsert target changes and its phone normalisation
                becomes THE shared normalizePhone the gate also uses. Import
                gains: dedupe against existing accounts, a per-row skip reason,
                and partial success (47 in, 3 to fix) instead of all-or-nothing.
  targets-crm   reads the imported book. After phase 2 an audience is built from
                PRODUCT TRACKS and account facts, not a flat contact list, which
                is what makes "fetch every customer onboarded for this product"
                a query instead of a wish.
  segments.ts   reads the ledger for behaviour (replied, read, silent). It keeps
                working; engagements become a richer source than messages alone,
                so a segment can finally say "visited, no follow-up in 14 days".
  campaigns-crm the monitor gains what phase 0 produces: per-recipient gate
                decisions with a reason, send state from the outbox, and delivery
                attributed to the message. Today it renders «أُرسلت N من M» and
                DROPS the per-recipient failures the API already returns.
  templates.ts  constants become records (above). The 3-step wizard keeps its
                shape; step 2 gains the approved-knowledge check, which stays
                behind phase 4 so it cannot block a flow that works today.

The wizard, the audience builder, the segments and the monitor are therefore
phase 6 work with a phase 2 dependency, and the campaign module is migrated,
not rebuilt.
```

---

## Design, specified

The first draft named screens and specified none. It also never referenced the thirteen finished
artboards at `_مسار/v2/`, which answer most of its open questions.

**Design source.** The artboards are binding storyboards; **`DESIGN.md` wins wherever they
disagree**, and they disagree in three known ways (rounded bordered list wrappers where the system
requires flush rows, filled status chips where state is a dot plus label, extra tinted surfaces
beyond the single teal accent). They are also all fixed at 1440x900 with **zero media queries**,
so they are not mobile references.

```
  PHASE                 ARTBOARDS
  spine (2)             Handoff · Duplicates · SalesInbox
  surfaces (3)          Main · Account · Outcome
  product hub (4)       Products · ProductKnowledge · ProductForSales
  control plane (6)     Campaign · CampaignMonitor · Conversation
  decks (8)             PitchDeck
```

**Four roles, each with a landing screen and its first three elements.** The artboards show four
personas, not three, and the live product already disagrees with itself about home (default route
is `#kmon`, the nav's first item is «الرئيسية»).

```
  ROLE            LANDS ON     FIRST              SECOND             THIRD
  مندوب (mobile)  #today       what must I do now who has gone quiet what is new for me
  مدير المبيعات   #inbox       overdue / unowned  who is behind      pipeline by stage
  إدارة المنتج    #outcome     what happened to   what customers     which product moves
                               what I handed off  said, verbatim
  admin           #gate        gate decisions     template health    audit
```
**Navigation is filtered by role, never by table.** Without this rule a 25-item rail exposing the
schema becomes the default.

**Every state is specified, because on day one every screen is empty.**
DESIGN.md's honest-absence vocabulary («لم تُرسل بعد» · «بلا جمهور» · «—») gains two entries the
gate needs: «مُنع» (refused) and «أُوقف تلقائيًا» (breaker). Every empty state names why it is
empty, who can change that, and the one action that does. «لا توجد بيانات» is not a design.

**The rep's flow is phone-first**, because a rep logging a visit is standing in a clinic holding a
phone. Tested at 360/390/430 CSS px, one-handed, 44px targets, no horizontal table scrolling,
account context pinned, **outcome first and note second**, the engagement **saved before** the
follow-up is generated, drafts surviving interruption, and a visible difference between «حُفظ» and
«أُرسل» — a rep who believes a follow-up was sent when it was only saved will not chase it.

**The rep's drafted follow-up ships in phase 3, but its grounding (approved claims) arrives in
phase 4.** Until then it is labelled «مسودة — راجعها قبل الإرسال».

**Arabic rules, as acceptance criteria.**
- Arabic-Indic numerals for **quantities a person counts**. Latin, `dir="ltr"`, monospace and
  copy-on-click for **identifiers a machine assigned** — `provider_msg_id`, `meta_template_id`,
  `idempotency_key`, phone numbers, match scores. `check:numerals` gains an allowlist for that
  class. A Gupshup message id rendered ٤٥٦٧٨ cannot be pasted into a support ticket.
- Counted nouns are four-way. `pluralizeArabic` exists in `opps-domain.ts:183` and
  **`dashboard.ts` calls it zero times in 4,127 lines**. The retrofit is a line item in phase 3,
  and a check flags any number concatenated with an Arabic noun.
- Every enum value gets an Arabic singular, dual, plural and sentence form, keyed by
  **(column, value)** — never by value alone. `manual` means two unrelated things, `other` means
  three, and `product`/`sales` sit on two different axes. Asserted at boot, following the existing
  `PROP_KEYS`/`PROP_LABELS` pattern in `tracker.ts`.
- Mixed Latin inside Arabic is isolated with `<bdi>`. There are **zero** in the codebase today,
  and phase 4 is a file-upload screen whose primary content is mixed filenames.
- Dates pin the calendar explicitly and all SLA arithmetic anchors to `Asia/Riyadh`.
- One state dimension per list row. Where two are genuinely simultaneous, one combined token
  («معتمد · جودة متدنية»), never stacked chips.

**Accessibility is acceptance criteria, not aspiration:** keyboard traversal with visible focus,
logical focus order in RTL, focus trap and restore, screen-reader announcements on save, failure
and async extraction, status never encoded by colour alone, reduced motion, 200% reflow. The
artboards are built from styled `div`s and must not be used as implementation references for
controls.

---

## Plan

## The first eight weeks (the pitch slice, decided 2026-09-04)

The founder committed to building the spine for real and then pitching, in eight weeks. The phase
table below does not deliver that: `0a + 0 + 1 + 2` is fifteen weeks and nothing in it is visible
to a sales director. **The eight weeks are re-cut around the pitch. The architecture is unchanged;
the order is.**

| Weeks | What | Drawn from |
|---|---|---|
| 1 | Migration system only — numbered files, a version table, an advisory lock, a Fly `release_command`, `pg_dump` | phase 0a. **Not optional**: week 2 alters a live CHECK constraint |
| 2–3 | The eight weighted stages (all six enum sites plus the browser bundle), `track_stage_events`, `actions` with `الإدارة المسؤولة` | phase 2 |
| 4–5 | Targets, sectors, `attainment_pct` and `coverage_pct` computed from the ledger, one grouped read query | phase 3 |
| 6–8 | Four screens: the rep's day (phone), the account record, the executive targets view, «أين تتعثّر الصفقات» | phase 3 |

**Deferred until after the pitch, deliberately:** the send gate and outbox (phase 0), sessions,
roles and SSO (most of phase 2), the product hub, agent quality, the WhatsApp control plane.

### The deferral is a mechanism, not a promise

Deferring phase 0 reverses "safety before features", which eight independent voices agreed on
yesterday. The defence is narrow and it must stay narrow: **the gate protects production sending,
and sending is disabled.** "We will do the gate afterwards" is what every team says, so it is
written as a condition rather than an intention:

> **The zero-send rule does not lift until phase 0 ships.** No production WABA number, no
> `SEND_ENABLED`, no pilot, no "just one test to my own phone" — until the gate consolidates the
> 13 bypassing call sites, `check-onedoor.mjs` is in `npm run check`, and the suppression backfill
> is proven. Phase 9 already depends on the founder lifting that rule; this makes phase 0 the only
> thing that can lift it.

That is enforceable by a build script rather than by memory, which is the whole lesson of
`outbound.ts`: a policy everyone must remember is a policy that has already failed here once.

| Phase | What ships | Proof | Weeks |
|---|---|---|---|
| **0a** | Foundations: migration system with a release command and `pg_dump`, Testcontainers, CI, coverage glob, SIGTERM handler, fetch timeout, honest `/health` | A deliberately broken migration fails the deploy instead of booting memory-only; a concurrency test can be written at all | 3 |
| **0** | The one door: gate consolidation, kill switch, suppressions with backfill, outbox with a real dispatcher, durable inbound events, audit with an operator surface. **Sending stays disabled.** | Bypassing call sites **13 -> 0**, asserted by `check-onedoor.mjs`; replaying all history sends nothing and logs a decision per attempt; two concurrent launches produce one outbox row; the gate refuses when Postgres is down | 3 |
| **1** | Design: role-to-landing map, the five-state matrix per surface, Arabic/RTL rules as DESIGN.md amendments, the phone-first rep flow | Every screen in phases 3-8 has a specified empty state before it is built | 2 |
| **2** | Identity and the commercial spine, migrated expand-backfill-contract with one writer per table. **Plus the eight weighted stages replacing the six, the ~28-value dependent outcome taxonomy, and `الإدارة المسؤولة` on every engagement** | Four people log in with real sessions; a PM hands 50 accounts and 3 failures are individually fixable; one clinic runs two tracks under two departments with the visibility rule enforced; **a weighted forecast is computable for the first time, and pending actions filter by department** | 7 |
| **3** | Surfaces, in four slices: **3a** the rep's day (mobile) · **3b** the PM outcome view · **3c** the manager's inbox · **3d** targets and the management view (executive / sector / product, RAG, and «أين تتعثّر الصفقات» by department) | A rep logs a call **on a phone** in under 10s and gets a labelled draft; the PM view distinguishes «لم يُتواصل» from «بلا سجل تواصل»; **an executive sees target vs achieved per sector with every figure earned from the ledger** | 10 |
| **4** | Versioned product hub: multi-file upload, extraction to claims with page references, per-claim human approval, expiry, the agent/sales audience split | A PM approves a version and the agent's answers change on the next message; an injected instruction in a source PDF is refused at approval | 3 |
| **5** | Agent quality: shadow mode, retrieval with citations, regression evals, per-tool authorization | Every agent sentence traces to an approved claim; a retired version stops being quotable within one message | 3 |
| **6** | WhatsApp control plane AND the campaign module migrated onto the spine: production WABA readiness, template records with separated desired/observed/breaker state, write-time button validation, per-template quality and pacing, breakers, the wizard, the audience importer, behavioural segments, and a monitor that shows per-recipient gate decisions | A quality change from Meta pauses only that template; an operator-authored 21-char button title is refused at write and never crashes a boot | 4 |
| **7** | Profiling filled: enrichment at import, HIS/ERP vendor registry, coverage on the record | Coverage rises from near zero to a stated number; the agent stops asking what it knows | 2 |
| **8** | Decks from approved claims, with the critic, human review before publish | A deck cites every slide; a slide with no approved claim renders blank rather than invented | 2 |
| **9** | Progressive send enablement — **only if the zero-send rule lifts** | Delivery, replies, opt-outs and per-template quality observed on a real pilot | 2 |

**41 weeks of work (3+3+2+7+10+3+3+4+2+2+2). With a 15% buffer, plan on 47 weeks.**

**The number moved again, and it is the fourth time.** 29 → 38 → 41. Each move came from
discovering something real rather than from re-estimating: first the omitted prerequisites
(migrations, a test harness, CI, file storage), now the founder archive's targets model and stage
taxonomy. Phase 2 grows 6 → 7 for the stages, the outcome taxonomy and the department field.
Phase 3 grows 8 → 10 for the management view.

**Do not read 41 as the commitment.** The commitment the founder made on 2026-09-04 is the
**first eight weeks** — the spine built for real, then the pitch. `docs/designs/massar-vision-ux-and-pitch.md`
is the source of truth for those eight weeks and for what goes in front of a sales director.
This document is the architecture the eight weeks build inside, and the map of what comes after
IF the pitch lands. Phases 4 through 9 are unfunded until then.

The review made this **nine weeks longer**, not shorter. The first estimate omitted a migration
system, a test harness, CI and the file-storage decision — all gating — and gave four weeks to
rewriting every screen in a 4,127-line template literal under ADR-0001. The column is summed, not
estimated; three earlier drafts in this project quoted totals that did not match their own tables.

## The stop rule

Both predecessor documents carried a kill criterion. The first draft of this one carried none,
across 33 weeks. **A 44-week programme needs one more than a short one does.**

**Gate A — adoption, at the end of phase 3a (about week 16).** The rep queue and the PM outcome
view ship first within phase 3 so the window closes before 3b and 3c are built. Measured per
rep-day, never per track: if, three weeks after one named rep has the queue, that rep records
**fewer than three engagements on a median working day**, or **fewer than half the calls they say
they made** appear within 24 hours, phases 4 through 8 do not ship as designed. The programme
returns to the tap-only record and the sheet.

**Gate B — the send pilot (phase 9).** Only if the zero-send rule lifts.

**Neither gate can be assessed without The Assignment**, which remains unclaimed: reconstruct the
last three handoffs by hand and bring back how many accounts were never touched, the median days
from handoff to first contact, and how many reached a real conversation. Half a day of work, and
it is the cheapest item in this document.

## Economics

The first draft had none. Delivery, reply and opt-out rates are channel telemetry, not a business
case: a campaign can perform well on all three and produce no revenue.

Before phase 2, state a target for: cost per qualified conversation (Gupshup + Meta + model +
storage), reps' time saved per week, follow-up latency reduced, and deals recovered from abandoned
handoffs. **Every phase from 3 onward carries one number a Lean sales manager would repeat
unprompted.** Phase 2 does not pass on four logins; it passes on four people still logging in in
week three.

## The incumbent

**The shared Google Sheet.** It is free, familiar, and already open on both teams' screens, and
the first draft did not mention it once. A plan that avoids dual-write in software while creating
dual-entry in the organisation has moved the problem.

**Decision: Massar writes back to the sheet.** Its earned signals — last contact, stage, next
action, silence — land in the columns the team already opens, from phase 3a. Adoption is won by
being where people already are before it is won by being better.

## The operating model

An AI agent holding commercial conversations needs a named human for: escalation and its response
time, out-of-hours conversations, pricing and contractual questions, an incorrect or sensitive
answer, template approval, product-content approval and expiry, complaints and opt-outs, and
incident response. The data model has owners; the organisation does not. **Named before phase 9,
and before any real send.**

## What this deliberately does not build

A second application. A dual-write period. A generic workflow engine or visual pipeline builder.
A dashboard builder. Autonomous deck publishing. Autonomous outbound sending. A campaign scheduler
beyond the dispatcher — **which is explicitly carved out of that deferral**, because an outbox
without a consumer is a log, not a safety mechanism.

## Risks

| Risk | Mitigation |
|---|---|
| **Real customer numbers and verbatim transcripts sit in two PUBLIC repositories** (CLAUDE.md:71, founder's explicit decision Aug 24) | Owner and decision point named before phase 9. Every phase increases the volume. Production expansion and public transcripts are not indefinitely compatible |
| A new caller bypasses the gate | `check-onedoor.mjs` fails the build. The convention already failed here; the mechanism is the fix |
| The gate fails open on a database blip | Fail-closed is stated, tested, and check-scripted |
| Historical opt-outs are not backfilled | Backfill is the first migration; the gate reads both sources until proven equal |
| A re-run sends twice | The key formula is written down and unit-tested; campaign creation is itself idempotent |
| A timeout re-sends | Timeouts classify as `unknown`, never `failed`; reconciliation from the delivery webhook |
| An operator's button title crashes every boot | Validate at write, refuse at runtime. Phase 6's first deliverable |
| One template degrades while the number looks healthy | Separated observed/breaker/desired state, per-template events, delivery attributed to the message |
| Extending v1 destabilises a working product | **The coverage gate covers 6.4% of lines today.** 0a widens it to a glob; every phase's rules land inside it |
| The migration breaks the only database | Numbered migrations, advisory lock, release command, `pg_dump`, expand-backfill-contract |
| The agent quotes retired or injected material | Versioned claims, per-claim human approval, injection eval |
| Nobody uses it | Gate A, and the sheet write-back |

## The one open question

Every voice in the review — two CEO, two design, one DX, two engineering, and this document's own
author — reached the same structural conclusion, with **zero disagreement across 19 scored
dimensions**: the architecture is right, and a 44-week programme that produces its first external
evidence in month four is the wrong shape for a team that has shipped one feature in the last
fifteen days.

Two of them proposed the same alternative independently: **an eight-week wedge** — the one-door
gate, the handoff ledger, the rep queue with one-tap outcomes, the PM outcome view, and the sheet
write-back — measured against Gate A, and only then a decision about whether the remaining 30
weeks are the right 30 weeks.

**That is not auto-decided here.** The founder asked for the full platform in his own words and
reaffirmed it when challenged. This document therefore keeps the full scope and adds the gate, so
the wedge is the *first sixteen weeks of the same plan* rather than a different plan. If he wants
the shorter shape, stopping at Gate A is already the design.

## What I noticed

- The first draft's central claim was wrong, and finding that out made the plan better. The gate
  exists. The work is connecting it, not inventing it — and that is cheaper and stronger.
- Three of eight voices independently found `outbound.ts`. One of them found the guard script that
  has been printing the bypass on every build for three weeks. Nobody had read it.
- The review made the schedule longer. That is what an honest review does to an estimate that
  omitted its own prerequisites.
- Five defects that are live in production today were found by checking claims about a plan. The
  most serious is that an inbound «إيقاف» can be lost, and it costs less to fix than to describe.

---

## GSTACK REVIEW REPORT

| Run | Skill | Date | Status | Findings |
|---|---|---|---|---|
| 1 | `/autoplan` (CEO · design · DX · eng) | 2026-09-03 | issues_open | 19 of 19 scored dimensions adverse, zero disagreement across 8 voices |
| 2 | `/plan-eng-review` | 2026-09-04 | **clean** | 5 findings on today's additions, all folded in; 1 scope decision; 1 open question |

### Run 2 — scope

Reviewed the parts added on 2026-09-04 and therefore unreviewed: the eight weighted stages, the
outcome taxonomy, `الإدارة المسؤولة`, and the targets model. Step 0's complexity check triggered
(4 new tables, a live CHECK alteration, 86 stage call sites) and stopped for a scope decision.

**Scope decision (founder, 2026-09-04):** re-cut the first eight weeks around the pitch. The
41-week architecture is unchanged; `0a + 0 + 1 + 2` no longer runs first. The send gate is
deferred behind an explicit condition rather than an intention.

### Run 2 — findings

| # | Severity | Finding | Resolution |
|---|---|---|---|
| 1 | **Critical** | The 6→8 stage migration was not executable. The enum is duplicated in **six** places including one **serialised into the browser bundle** (`opps-domain.ts:221`), and `CREATE TABLE IF NOT EXISTS` alters none of them; the live `CHECK` (db.ts:263) must be dropped and recreated | Mapping written out — six keys preserved 1:1, `discover` and `quote` new, so no live row is reclassified. All six sites named and sequenced. `negotiate` flagged for audit before backfill |
| 2 | **Critical** | The stage outcome sat on `engagements`, which is the wrong entity. An engagement is evidence; an outcome is a decision about one track at one stage. And `responsible_dept` on an append-only engagement records who handled a *historical* interaction — so «أين تتعثّر الصفقات» would have listed every department that ever touched the deal | Split into `track_stage_events` (append-only, `outcome.stage_id = from_stage_id`) and `actions` (current responsibility, due date, state) |
| 3 | **Critical** | **`achieved` was not computable at all.** Verified: `stage_at` (db.ts:286) is the last move and is overwritten (db.ts:1021); `close_on` (db.ts:280) is a *planned* date. The engine cannot answer "which deals were won in Q1" — the question the entire targets model rests on | The same `track_stage_events` ledger. Phase 3 was unbuildable without it |
| 4 | **High** | "Computed" was specified with no read architecture, on a 512 MB machine with pool max 5 where `/health` already runs three exact `COUNT(*)` every 30s (db.ts:479) | One grouped query per period feeding every tile from one snapshot; four indexes named; health counts replaced with a connectivity check |
| 5 | **High** | Quarter boundaries and the RAG numerator were both undefined. A win at 23:30 Riyadh on 31 March is 20:30Z and lands in the wrong quarter if read in UTC | Half-open Riyadh-local boundaries; bucket on `effective_at`; `attainment_pct` and `coverage_pct` named separately, RAG driven by attainment against the *elapsed* share of the period |

**Corroboration worth recording:** `OPP_STALL_STAGES` (`opps-domain.ts:53`) is already hardcoded to
`["tech","negotiate"]` — the same two stages the founder archive independently identifies as the
longest-stalling. The prototype found it months before the requirements document was recovered.

**What the additions get right, per the outside voice:** stages as data, weights making a weighted
pipeline possible, target facts entered while achieved and forecast stay reproducible derivatives,
target uniqueness by product and period, and status colour paired with text.

VERDICT: **CLEAR.** CODEX absorbed — five findings, all folded in; the outside voice and this
review reached findings 1, 4 and 5 independently.

**UNRESOLVED DECISIONS:**
- Fiscal calendar and target basis: does Lean's fiscal year start in January or another month, is a year labelled by its start or its end, and does a target measure **bookings**, **annual contract value**, or **total contract value**? `sale_price × qty × years` is TCV, and booking all of it into the win quarter is not achieved revenue. This is a business fact, it changes every number on the executive screen, and it cannot be resolved from the codebase.
