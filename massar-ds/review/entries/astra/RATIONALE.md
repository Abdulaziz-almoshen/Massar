# مسار — الرقم وحدوده

**One idea: an amount should never look more certain than its evidence.** The interface is a sales ledger with a very large zero. The founder first sees `0 ر.س`, then the boundary around `34,000 ر.س`: الإجازات المرضية, Q3, one product. Seven products have no recorded target. The five-second answer is therefore visible without a fabricated attainment rate: no won revenue, and insufficient target coverage to judge company attainment.

Rules divide the page; red identifies actual gaps. The next reading stop is five unpriced lines, then the 3–24-day stagnation range. The eight-slot strip measures target coverage. The campaign register preserves all six counts without making them a conversion analysis. The knowledge score remains separate from completed sections. The home page includes every supplied fact.

I deliberately omitted a cash-collected claim: the supplied financial field is won revenue. I also omitted charts of invented history, forecast probabilities, monthly targets, progress towards a company number, fake customers, fake product names, and row-level stage or day assignments. There are no KPI cards, sidebar, avatar, greeting, decorative icons or celebratory motion. The amount receives more space than the chrome.

**The ledger now resolves the six real opportunity lines.** The addendum replaces the earlier evidence gap: each row retains its supplied number, account, product, stage, price, stationary days, source, last activity, owner and sector, including repeated accounts. Compact ruled rows put the longest stationary lines first; every column sorts, stages follow the supplied ladder, and unknown prices stay last in either direction. Pricing, stage and account/product search combine; the visible count and priced subtotal follow the result, while the headline retains the complete population. A salesperson can open an individual record or export the current view. No stage or duration is inferred, and activity dates remain independent of stationary days. At 400px every row reflows into labelled fields, keeping all columns' data visible and sorting accessible. The remaining compromise is a longer mobile list; the fixed snapshot supports inspection, filtering and export, with no simulated updates to production.

The six empty states have distinct labels and treatments in the state dictionary. Only supported states appear against actual data; the design does not invent unclassified products to demonstrate a component. On a 400px screen the ledger keeps all columns visible, with compact supporting text as the main readability compromise.

Both HTML files contain their own fonts, CSS and JavaScript and run offline. Desktop and 400px Chrome checks passed for layout, filter counts, numeric isolation, dialog focus and reduced motion. Real phone input behaviour remains unverified.
