live-ops-dashboard (/dashboard) — TRIVIAL: recomposition of approved design-language idioms only (§4 KPI/chip/table/WA-preview + §5 campaign-detail contract incl. seen = read ∪ replied); zero new tokens/layout/interaction patterns; internal single-viewer ops page; no loop cycle active (idle, 2026-08-10); build-to spec delivered by designer.
