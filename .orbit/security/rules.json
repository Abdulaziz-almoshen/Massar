[
  {
    "decision": "deny",
    "reason": "`fly apps destroy` deletes the deployed massar-engine irreversibly — forbidden from the loop.",
    "match": { "argv_regex": "\\bfly(ctl)?\\b.*\\bapps\\b.*\\bdestroy\\b" }
  },
  {
    "decision": "deny",
    "reason": "unsetting Fly secrets would break the live engine's keys — forbidden; rotate via `fly secrets set` instead.",
    "match": { "argv_regex": "\\bfly(ctl)?\\b.*\\bsecrets\\b.*\\bunset\\b" }
  },
  {
    "decision": "ask",
    "reason": "a direct Gupshup API call from the shell can send a real WhatsApp message — outbound sends need human approval.",
    "match": { "argv_regex": "api\\.gupshup\\.io" }
  },
  {
    "decision": "ask",
    "reason": "the engine's /admin/send endpoints message real phones — confirm the recipient is an opted-in team number.",
    "match": { "argv_regex": "massar-engine\\.fly\\.dev/admin/send" }
  },
  {
    "decision": "ask",
    "reason": "deleting a .env would lose the local secrets — confirm.",
    "match": { "argv_contains": ["rm"], "argv_regex": "\\.env(\\b|$)" }
  }
]
